/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['images.unsplash.com', 'internaltest0.advanz.no'],
  },
}

module.exports = nextConfig
